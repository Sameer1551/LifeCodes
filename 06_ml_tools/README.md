# 06_ml_tools

## Purpose
Machine learning helper functions and logger setup configuration.

## Files
- `logger_setup.py` (logging initialization for ML experiments)
- `quickclidemo.md` (usage demo)


## What these files do
logger_setup.py configures ML-appropriate logging for experiments and model results.

This section explains purpose-level behavior and how they support your project.

## Importance
This section is important for building and running project components reliably.
## How to run
1. Install Python 3.8+ and optional dependencies (`numpy`, `pandas`, etc.).
2. Open terminal in `06_ml_tools`.
3. Run `python logger_setup.py`.
4. Review `quickclidemo.md` for CLI examples.

